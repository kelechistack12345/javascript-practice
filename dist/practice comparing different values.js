function compareequality(a,b){
    if(a === b){
        return "it equal"
    }
        return "not equal"
}

console.log(compareequality(10,10));

// with the triple equality sign it has to exactly equal data.

function compareequality(a,b){
    if(a === b){
        return "it equal"
    }
        return "not equal"
}

console.log(compareequality(10,"10"));